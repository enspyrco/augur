# Augur

People-intelligence engine — the `social` CLI evolved to absorb `rolo` (breadth + depth in one loop):
`discover -> dig -> fuse -> refute -> place -> weave`, plus a serendipity daemon and reverse-casting.
Rehomed here from an imagineering/community session (2026-07-02).

- **Design:** `AUGUR-DESIGN.md` (full synthesis of an 18-agent research sweep; consent/legal spine up front).
- **Build order:** `dig` first (typed-identity-graph BFS with a corroboration gate + consent-tier depth budget).
- **Related:** the `/recombine` skill (claude-skills) — the Egregore Engine is Augur's flagship recombination.
